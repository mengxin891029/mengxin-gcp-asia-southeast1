apiVersion: v1
kind: Service
metadata:
  name: sea-mengxin-ml-clusterip
  namespace: web
spec:
  selector:
    pod: sea-mengxin-ml
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
